# Binds `next dev` to 3001 (matches package.json). Do not use 3000 here.
PORT=3001

# Browser + server fetch target (api-gateway default in this monorepo).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4010
