# Cursor Start Here

Paste this into Cursor first:

```text
We are building a roadmap management platform from the docs in this repo.
Use README.md, services/portfolio-service/prisma/schema.prisma, packages/types/src/index.ts, and the web app as the current source of truth.
Your job is to continue implementation without changing the monorepo architecture.
Focus next on:
1. make all services install and build cleanly
2. add full CRUD + update/delete endpoints
3. wire templates
4. add spreadsheet import review workflow
5. add timeline rendering and quarter grouping
6. add Jira and Confluence integration models
7. add background jobs with Redis/BullMQ
8. containerize app services fully
Keep changes practical and production-oriented.
```
